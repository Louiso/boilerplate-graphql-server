type Mutation {
  updateProfileBasicInformation(
    input: UpdateProfileBasicInformationInput!
  ): Profile! @auth

  updateCV(
    input: UpdateCVInput!
  ): Profile! @auth
  
  sendProfile(jobId: ID!): Profile! @auth
  
  updateExperience(
    input: [ExperienceInput]
  ): Profile @auth
}