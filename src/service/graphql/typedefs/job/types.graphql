type CompanyProfile {
  _id: ID
  logo: String
}

type Company {
  _id: ID
  name: String
  photo: String
  slug: String
  profile: CompanyProfile
  theme: JSON
  premium: Boolean
}

type Job {
  _id: ID!
  stages: [ Stage! ]!
  title: String!
  companyPublished: Company
  publications: [Publication!]
}

type Publication {
  _id: ID!
  title: String
}