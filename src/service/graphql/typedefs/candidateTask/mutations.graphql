type Mutation {
  createResultTask(
    candidateTaskId: ID!
    taskId: ID!
    jobId: ID!
  ): CandidateTask! @auth

  notifyOpenTaskInDesktop(
    taskId: ID!
    jobId: ID!
    publicationIndex: Int!
    slug: String!
  ): Candidate! @auth
}