type Mutation {
  createResultTask(
    candidateTaskId: ID!
    taskId: ID!
    jobId: ID!
  ): CandidateTask! @auth

  nofifyOpenTaskInDesktop(
    taskId: ID!
    jobId: ID!
    publicationIndex: Int!
    slug: String!
  ): Candidate! @auth
}