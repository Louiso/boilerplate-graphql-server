type Query {
  getCandidate(
    jobId: ID! 
    publicationIndex: Int
    slug: String
    trackingCandidateId: ID
  ): Candidate! @auth

  getCandidateById(
    candidateId: ID!
  ): Candidate! @auth
}